# 联调验收单 · 门户侧（Mac 192.168.3.96）

_2026-09-22 23:00 · 对照《联调结果-诗词侧-2026-09-22.md》回执逐项复核_

---

## 1. 对方回执声明复核 —— 全部属实 ✅

| 回执声明 | 我方独立复核（从 .96 实测） | 结论 |
|---|---|---|
| P0 自检 PASS 6/6（v2 脚本） | 回执附原始输出，与 v2 设计一致 | ✅ 认可 |
| xmlrpc → 403 | POST /xmlrpc.php → **403** | ✅ |
| WP users → 401 | GET /wp-json/wp/v2/users → **401**（jason 不再外泄） | ✅ |
| MySQL 3306 已停 | nc 探测 → **0 监听** | ✅ |
| Ollama 仅放行 .96 | .96 → **200**（白名单生效），策略符合方案 | ✅ |
| P2 赏析 231 字 ≥100 字验收线 | poem-appreciation.json 实测 **231 字**，meta（qwen2.5:3b / Windows .44 / 22:27:10）与声明一致；质量抽查通过（意象/情感/手法三段式完整） | ✅ **P2 验收通过** |

## 2. 我方回执：01-mac-selfcheck.sh（10 项）

```
PASS ×10 —— RESULT: PASS 10/10, FAIL 0
（门户 https / Looma /v1/health / menus×7 / Claw 监听 / 中继 4443 /
 对方站点 / 反代取诗 / 对方 Ollama / 本机 7b / bge-m3）
```

## 3. v3 反馈已落实

- `02-peer-checks.sh` / `.ps1` 所有 curl 统一加 `--noproxy '*'`（各 6 处）
- 回归实测：跨机三项（门户/契约/camelCase）严格 TLS 下仍全 PASS
- 本次回执（本文件）+ v3 脚本已同步更新交付包

## 4. 联调阶段结论

| 阶段 | 状态 |
|---|---|
| P0 连通性互测 | ✅ **双方验收通过**（.44 6/6 ＋ .96 10/10，严格 TLS 双向） |
| P1 内容互通 | ✅ 反代（.96→.44）+ 契约（.44→.96）双向只读打通 |
| P2 AI 赏析 PoC | ✅ **验收通过**（231 字，数据不出局域网） |
| P3 自动化 | ⏳ 待对方交付 WP Application Password（≤publish_posts）后启动 |

## 5. P3 准备（门户侧承诺事项）

1. 门户新增「诗词」页：列表消费 `/peer-wp-json/wp/v2/posts`，点进详情
2. Zeroclaw agent 赏析编排：新诗 webhook → qwen2.5:7b 赏析 → 记忆沉淀 → 回写 WP 草稿
3. bge-m3 诗词语义检索（P3 后半，视 P3 前半落地情况）

_门户侧签收：P0/P1/P2 验收通过，进入 P3 准备。_
